<!DOCTYPE html>
<html>
<head>
	<meta charset="UTF-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1">

	<title>{{title}}</title>

	{{>styles}}

	<style>
		body {
			margin:0;
			padding:0;
			overflow: hidden;
		}
		canvas {
			margin:auto;
		}
	</style>
</head>
<body>
	{{>header}}

    <div class="products">
		<div class="container text-center">
			<div class="row">

				<div class="col-sm-3">
					<h1><a href="#filters" data-toggle="collapse">Филтри</a></h1>

		      	<div id="filters" class="collapse">
					<h4>Молим Ви да ни извините, функцията предстои да бъде добавена!</h4>
					{{!-- <h4>Нещо</h4>
					<p>може</p>
					<p>би</p>
					<p>радио</p>
					<p>бутони</p> --}}
				</div>

				</div>

				<div class="col-sm-9">
					<div class="row">
						{{#each products}}

						<a href="./doctors/{{url}}">
							<div class="col-sm-4">
								<img id="thumbnail" src="./medias/{{medias.0.id}}" alt="{{medias.0.alt}}" title="{{medias.0.alt}}" class='cropmedia thumbnail' width="150">
								<h1>{{name}}</h1>
								<p>{{rating}}</p>
							</div>
						</a>
						{{/each}}
						
					</div>
				</div>

			</div>
		</div>
	</div>

	{{>footer}}

</body>

{{>scripts}}

</html>
