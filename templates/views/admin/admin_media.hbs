<!DOCTYPE html>
<html>
<head>
	<meta charset="UTF-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1">

	<title>{{title}}</title>

	{{>admin_styles}}

	<style>
		body {
			margin:0;
			padding:0;
			overflow: hidden;
		}
		canvas {
			margin:auto;
		}
	</style>
</head>

<body>
	<div class="wrapper">
		{{>admin_navigation}}
		
		<div class="scrollable">
			<div id="content-wrapper" class="container-fluid">
				<div class="row">
					<div class="media-options" style="margin-left: 1%; margin-top: 1%;">
						{{>admin_media_upload}}
						<a href="javascript:history.back()"><i class="fas fa-arrow-left"></i>Return to previous</a>
						<button type="submit" class="btn btn-primary" id="delete"><i class="fas fa-trash-alt"></i> Delete Selected</button>
						<button type="submit" class="btn btn-primary" id="refresh" style="background: #620CE8;"><i class="fas fa-sync-alt"></i> Refresh Media</button>
						<button type="submit" class="btn btn-primary" id="use" style="visibility: hidden"><i class="fas fa-arrows-alt"></i> Use Selected</button>
						<p class="count">0 selected items</p>
					</div>
				</div>
				<div class="row">
					{{#each medias}}
						<div class="col-xs-4 col-sm-4 col-lg-3">
							<img src="/medias/{{_id}}" id="{{_id}}" class="cropmedia">
							<h5 class="text-center">{{filename}}</h5>
							<p class="text-center">{{alt}}</p>
						</div>
					{{/each}}
				</div>
			</div>
		</div>
		<script src="/js/admin/admin_media.js"></script>
		
	</div>
</body>
{{>admin_scripts}}

</html>
